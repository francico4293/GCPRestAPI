'use strict';

const PUBLIC = 'public';

const HBS = '.hbs';
const VIEW_ENGINE = 'view engine';

const NUMBER_OF_RAND_BYTES = 16;

const HEX = 'hex';

const STATES = 'States';

module.exports = { 
    PUBLIC, 
    HBS, 
    VIEW_ENGINE ,
    NUMBER_OF_RAND_BYTES,
    HEX,
    STATES
};
