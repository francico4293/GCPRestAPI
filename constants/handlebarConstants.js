'use strict';

const HBS = '.hbs';

const VIEW_ENGINE = 'view engine';

const MAIN = 'main';

const LOGIN = 'login';

const ERROR = 'error';

const USER_INFO = 'userInfo';

module.exports = { 
    HBS,
    VIEW_ENGINE,
    MAIN, 
    LOGIN,
    ERROR,
    USER_INFO
};
