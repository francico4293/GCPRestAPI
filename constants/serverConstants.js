'use strict';

const PUBLIC = 'public';

const HOST = 'host';

const LOCAL_HOST = 'localhost';

const APPLICATION_JSON = 'application/json';

const CONTENT_TYPE = 'content-type';

const ANY_MIME_TYPE = '*/*';

module.exports = { 
    PUBLIC,
    HOST,
    LOCAL_HOST,
    APPLICATION_JSON, 
    CONTENT_TYPE, 
    ANY_MIME_TYPE
};
