'use strict';

const MIN_CAPACITY = 1;

const MAX_CAPACITY = 5;

const HANGAR_NAME_LOCATION_REGEX = /^[A-Za-z./,-\s0-9]+$/;

module.exports = {
    MIN_CAPACITY,
    MAX_CAPACITY,
    HANGAR_NAME_LOCATION_REGEX
};
