'use strict';

const STRING = 'string';

const NUMBER = 'number';

const INVALID_SPACING_REGEX = /\s+/;

const SPACE_CHAR = ' ';

module.exports = {
    STRING,
    NUMBER,
    INVALID_SPACING_REGEX,
    SPACE_CHAR
};
